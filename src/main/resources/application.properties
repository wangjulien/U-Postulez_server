# Spring Data properties
spring.datasource.url=jdbc:mariadb://localhost:3307/chinese_box_test
spring.datasource.username=root
spring.datasource.password=Wang_1981
spring.datasource.driverClassName=org.mariadb.jdbc.Driver

spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect


# Define a custom port instead of the default 8080
server.port=8443


# Cors host permission
security.cors.permit-host=localhost


# The format used for the keystore 
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:keystore.p12
# The password used to generate the certificate
server.ssl.key-store-password=Wang_1981
# The alias mapped to the certificate
server.ssl.key-alias=tomcat